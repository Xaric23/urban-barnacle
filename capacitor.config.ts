import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xaric23.undergroundclubmanager',
  appName: 'Underground Club Manager',
  webDir: 'out'
};

export default config;
